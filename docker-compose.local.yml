services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-local
      target: development
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      PORT: 3000
